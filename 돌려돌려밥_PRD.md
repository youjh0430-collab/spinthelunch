# 돌려돌려밥 🍚🎰

> "오늘 점심 뭐 먹지?" — 룰렛 한 번이면 끝.

---

## 1. 개요 (Background)

- **배경**: 직장인·학생 할 것 없이 매일 반복되는 "점심 뭐 먹지?" 결정 피로. 검색하면 너무 많은 선택지, 결국 어제와 같은 메뉴를 먹게 되는 악순환.
  - 직장인 72%가 점심 메뉴 선택에 평균 10분 이상 소요 (2024 직장인 식사 트렌드 리서치, 배달의민족)
  - "오늘 뭐 먹지" 네이버 검색량 월평균 약 12만 건 (네이버 데이터랩 2024 기준)
- **해결**: 위치 기반으로 주변 맛집을 불러온 뒤, 사용자가 설정한 조건(카테고리·거리)에 맞는 식당을 **룰렛으로 랜덤 추천**하여 결정 피로를 제거.
- **기간**: 1주일 (직장 업무 외 여유 시간 활용)

---

## 2. 경쟁 분석

| 서비스 | 특징 | 한계 |
|--------|------|------|
| **배달의민족 룰렛** | 앱 내 메뉴 룰렛 기능 | 배달 주문 전제, 주변 오프라인 식당 탐색 불가 |
| **점메추 (앱)** | 메뉴 카테고리 기반 랜덤 추천 | 위치 기반이 아님 — "한식"만 추천하고 어디서 먹을지는 알려주지 않음 |
| **네이버/카카오 지도** | 풍부한 식당 데이터, 리뷰 | 선택지가 너무 많아 결정 피로를 오히려 가중 |
| **식신, 망고플레이트** | 맛집 큐레이션, 평점 기반 | 탐색·비교 과정이 필요 → 빠른 결정에 부적합 |

**차별점**: 돌려돌려밥은 **"탐색 없는 결정"**에 집중합니다.
- 위치 기반 + 조건 필터 → 룰렛 1회로 식당 확정
- 별도 앱 설치 없이 모바일 웹에서 즉시 사용
- 리뷰·평점 비교 과정을 의도적으로 생략하여 결정 속도 극대화

---

## 3. 타겟 사용자

| 구분 | 설명 |
|------|------|
| **주 타겟** | 매일 점심 메뉴 고민하는 직장인 |
| **보조 타겟** | 학교 주변 식당 탐색하는 대학생, 여행지에서 식당 고르기 귀찮은 여행객 |
| **핵심 페인포인트** | 선택지가 너무 많아서 오히려 못 고르는 "결정 장애" |

---

## 4. 사용자 가치 (User Value)

- **결정 피로 제거**: 조건만 설정하면 룰렛이 대신 골라줌
- **새로운 식당 발견**: 평소 안 가던 식당을 자연스럽게 접할 기회
- **재미 요소**: 룰렛 UI로 일상적인 식사 선택에 게임적 즐거움 부여

---

## 5. MVP 스펙 (Phase 1)

### 5.1 사용자 플로우

```
접속 → 현재 위치 자동 감지 → 카테고리 선택 → 반경 설정 → 룰렛 돌리기 → 결과 확인 → 상세 보기(외부 링크)
```

### 5.2 핵심 기능

> **우선순위 기준**: P0 = 없으면 서비스 불가 / P1 = MVP 완성도에 필수 / P2 = 있으면 좋지만 후순위 가능

#### F1. 현재 위치 기반 지도 렌더링 `P0`
- 접속 시 브라우저 Geolocation API로 현재 위치 감지
- 네이버 지도 또는 카카오맵에 현재 위치 표시
- 위치 감지 실패 시 수동 주소 입력 fallback

#### F2. 필터 설정 `P0`
- **카테고리 선택** (다중 선택 가능)
  - 한식, 중식, 일식, 양식, 분식, 카페/디저트, 패스트푸드, 기타
- **반경 설정**
  - 300m / 500m / 1km / 2km (기본값: 500m)
- 필터는 상단 또는 사이드 패널에 항상 노출

#### F3. 룰렛 추천 `P0`
- 필터 조건에 맞는 주변 식당 목록을 API로 조회
- 조회된 식당들을 룰렛 UI에 배치
- "돌려!" 버튼 클릭 시 룰렛 애니메이션 → 1개 식당 선택
- 결과가 마음에 안 들면 "다시 돌리기" 가능

#### F4. 결과 카드 `P1`
- 선택된 식당 정보 표시:
  - 식당명
  - 카테고리
  - 주소
  - 현재 위치에서의 거리
  - 전화번호
- **"자세히 보기"** 버튼 → 네이버 지도 / 카카오맵 상세 페이지로 이동 (딥링크)
- **"지도에서 보기"** 버튼 → 지도에 해당 식당 마커 + 경로 표시 `P2`

### 5.3 예외 시나리오

| 시나리오 | 조건 | 처리 방안 |
|----------|------|----------|
| **검색 결과 0건** | 필터 조건에 맞는 식당이 없음 | "조건에 맞는 식당이 없습니다. 반경을 넓히거나 카테고리를 추가해보세요." 안내 + 필터 재설정 유도 |
| **검색 결과 1~2건** | 룰렛에 넣기엔 너무 적음 | 룰렛 대신 카드 리스트로 바로 표시 ("주변에 N개 식당만 있어요!") |
| **검색 결과 과다 (45건+)** | 카카오 API 페이징 한도 도달 | 최대 45건까지만 사용, 거리순 정렬 후 가까운 식당 우선 배치 |
| **API 호출 실패** | 네트워크 오류, 카카오 서버 장애 | "식당 정보를 불러오지 못했습니다. 잠시 후 다시 시도해주세요." + 재시도 버튼 |
| **위치 권한 거부 후 수동 입력도 실패** | 주소 검색 결과 없음 | "주소를 찾을 수 없습니다. 다른 주소로 다시 검색해주세요." 안내 |
| **동일 식당 연속 당첨** | 재돌리기 시 같은 식당이 다시 선택됨 | 직전 결과를 제외하고 룰렛 구성 (최소 2개 이상일 때) |

### 5.4 화면 구성

| 화면 | 설명 |
|------|------|
| **메인** | 지도 배경 + 상단 필터 바 + 하단 "돌려!" 버튼 |
| **룰렛 모달** | 버튼 클릭 시 풀스크린 룰렛 애니메이션 |
| **결과 카드** | 룰렛 종료 후 하단에서 올라오는 식당 정보 카드 |

---

## 6. 기술 요구사항

### 6.1 API

| API | 용도 | 필요 데이터 |
|-----|------|------------|
| **카카오 로컬 API** (키워드/카테고리 검색) | 주변 식당 목록 조회 | 장소명, 카테고리, 주소, 전화번호, 위경도, place_url |
| **카카오맵 JavaScript API** | 지도 렌더링, 마커, 경로 | - |
| **브라우저 Geolocation API** | 현재 위치 좌표 획득 | 위도, 경도 |

> **참고**: 카카오 로컬 API는 카테고리 검색(FD6: 음식점)을 지원하여, 음식점 카테고리 세분류(한식, 중식 등)로 필터링이 가능합니다. 네이버 검색 API 대비 구조화된 데이터 제공이 장점입니다.

### 6.2 프론트엔드

- **프레임워크**: React + Vite (또는 Vanilla JS — 별도 결정 필요)
- **룰렛 UI**: Canvas API 또는 CSS 애니메이션
- **반응형**: 모바일 우선 설계 (점심시간에 스마트폰으로 사용하는 시나리오)

### 6.3 백엔드

- **Phase 1에서는 백엔드 없음**
- 카카오 API 호출은 프론트엔드에서 직접 수행
- 배포: Vercel 또는 GitHub Pages

### 6.4 API 키 보안

| 위협 | 대응 |
|------|------|
| 브라우저 DevTools에서 API 키 노출 | 카카오 REST API 키는 **플랫폼 도메인 제한** 설정으로 등록된 도메인에서만 유효하도록 제한 |
| 키 탈취 후 타 도메인에서 악용 | 카카오 개발자 콘솔에서 허용 도메인을 배포 URL(예: `*.vercel.app`)로 한정 |
| 일일 쿼터 소진 공격 | 카카오 API 기본 쿼터(일 30만 건) 내에서는 MVP 수준 트래픽으로 문제 없음. 이상 트래픽 감지 시 Vercel Rate Limit 미들웨어 도입 검토 |
| Phase 2 이후 확장 시 | 프록시 서버(Vercel Serverless Function) 도입하여 API 키를 서버 사이드로 이전 |

### 6.5 비기능 요구사항 (NFR)

| 항목 | 기준 |
|------|------|
| **초기 로딩** | First Contentful Paint 2초 이내 (모바일 4G 기준) |
| **룰렛 인터랙션** | 버튼 클릭 후 애니메이션 시작까지 100ms 이내 |
| **브라우저 호환** | Chrome, Safari, Samsung Internet 최신 2개 버전 (모바일 우선) |
| **반응형** | 320px ~ 430px (모바일), 768px 이상(태블릿/데스크톱) 대응 |
| **접근성** | 룰렛 결과를 `aria-live`로 스크린리더에 전달, 버튼 최소 터치 영역 44×44px |
| **오프라인** | 미지원 (네트워크 필수 — 오프라인 시 안내 메시지 표시) |

---

## 7. Phase 로드맵

| Phase | 기능 | 비고 |
|-------|------|------|
| **Phase 1 (이번 주)** | 위치 감지 + 카테고리/반경 필터 + 룰렛 추천 + 결과 카드 | MVP |
| **Phase 2** | 즐겨찾기(로컬 저장), 식당 제외(블랙리스트), 최근 추천 히스토리 | 재방문 유도 |
| **Phase 3** | 그룹 모드(여러 명이 각자 룰렛 → 겹치는 식당 우선), 공유 기능 | 소셜 확장 |

---

## 8. 성공 지표 (KPI)

| 지표 | 정의 | 목표 (MVP 출시 후 2주) | 측정 방법 |
|------|------|----------------------|----------|
| **룰렛 전환율** | 접속 사용자 중 룰렛 1회 이상 돌린 비율 | ≥ 60% | GA 이벤트 (`roulette_spin`) |
| **재돌리기 비율** | 룰렛 돌린 사용자 중 "다시 돌리기" 클릭 비율 | ≤ 40% (낮을수록 만족) | GA 이벤트 (`roulette_respin`) |
| **상세보기 클릭률** | 결과 카드 노출 대비 "자세히 보기" 클릭 비율 | ≥ 50% | GA 이벤트 (`detail_click`) |
| **세션당 룰렛 횟수** | 1세션에서 평균 룰렛 돌린 횟수 | 1.5~2.5회 | GA 세션 집계 |
| **재방문율** | 7일 내 재접속 사용자 비율 | ≥ 20% | GA 코호트 분석 |

---

## 9. 제약 사항 및 리스크

| 항목 | 내용 | 대응 |
|------|------|------|
| 카카오 API 일일 호출 제한 | 기본 무료 쿼터 내 운영 | MVP 단계에서는 충분, 트래픽 증가 시 캐싱 도입 |
| 위치 권한 거부 | 지도 및 검색 불가 | 수동 주소 입력 UI 제공 |
| 카테고리 세분류 정확도 | 카카오 데이터의 카테고리 분류가 부정확할 수 있음 | 있는 그대로 사용, Phase 2에서 보정 검토 |
